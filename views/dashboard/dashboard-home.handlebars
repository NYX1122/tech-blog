<div class="container mt-5">
    {{#each posts as |post|}}
    <div class="row justify-content-start mb-3 bg-info border border-info rounded-3">
        {{> dashboard/dashboard-card post }}
    </div>
    {{/each}}
</div>